<Application x:Class="GlassForge.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <SolidColorBrush x:Key="PageBrush" Color="#B0101014" />
    <SolidColorBrush x:Key="PanelBrush" Color="#B819191F" />
    <SolidColorBrush x:Key="RaisedBrush" Color="#D022222A" />
    <SolidColorBrush x:Key="TextBrush" Color="#F4F2F8" />
    <SolidColorBrush x:Key="MutedBrush" Color="#A9A4B2" />
    <SolidColorBrush x:Key="AccentBrush" Color="#8B5CF6" />
    <SolidColorBrush x:Key="BorderBrush" Color="#603F3D48" />
    <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource TextBrush}" /></Style>
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="{StaticResource RaisedBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/><Setter Property="Padding" Value="14,8"/>
      <Setter Property="BorderThickness" Value="1"/><Setter Property="Margin" Value="4"/><Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/><Setter Property="Background" Value="{StaticResource RaisedBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/><Setter Property="Padding" Value="8"/>
    </Style>
    <Style TargetType="ListBox">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/><Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="{StaticResource RaisedBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>
    <Style TargetType="ComboBoxItem">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="{StaticResource RaisedBrush}"/>
      <Setter Property="Padding" Value="10,7"/>
      <Style.Triggers>
        <Trigger Property="IsHighlighted" Value="True">
          <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
          <Setter Property="Foreground" Value="White"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <ControlTemplate x:Key="GlassSliderThumb" TargetType="Thumb">
      <Grid Width="16" Height="16">
        <Ellipse x:Name="Glow" Fill="#408B5CF6" Width="16" Height="16"/>
        <Ellipse x:Name="Core" Fill="#A78BFA" Stroke="#EDE9FE" StrokeThickness="1.2" Width="11" Height="11"/>
      </Grid>
      <ControlTemplate.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter TargetName="Glow" Property="Fill" Value="#708B5CF6"/>
          <Setter TargetName="Core" Property="Fill" Value="#C4B5FD"/>
          <Setter TargetName="Core" Property="Width" Value="13"/>
          <Setter TargetName="Core" Property="Height" Value="13"/>
        </Trigger>
        <Trigger Property="IsDragging" Value="True">
          <Setter TargetName="Glow" Property="Fill" Value="#A08B5CF6"/>
          <Setter TargetName="Core" Property="Fill" Value="#FFFFFF"/>
        </Trigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style TargetType="Slider">
      <Setter Property="Height" Value="26"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Slider">
            <Grid Height="26" VerticalAlignment="Center">
              <Border Height="5" VerticalAlignment="Center" Background="#55101016" BorderBrush="#604A4654" BorderThickness="1" CornerRadius="3"/>
              <Track x:Name="PART_Track" VerticalAlignment="Center" Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" Value="{TemplateBinding Value}">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="Slider.DecreaseLarge" IsTabStop="False">
                    <RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Height="5" Background="{StaticResource AccentBrush}" CornerRadius="3,0,0,3"/></ControlTemplate></RepeatButton.Template>
                  </RepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="Slider.IncreaseLarge" IsTabStop="False" Background="Transparent" BorderThickness="0"/>
                </Track.IncreaseRepeatButton>
                <Track.Thumb><Thumb Template="{StaticResource GlassSliderThumb}"/></Track.Thumb>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Application.Resources>
</Application>
